# frontend/.env  — copia este archivo como ".env" (o configúralo en Vercel).
#
# URL base del backend (API). En local no hace falta (usa el default
# http://localhost:3000/api). En producción, apunta al backend en Render:
VITE_API_URL=https://tu-backend.onrender.com/api
