# ============================================================
#  Riwi-Projects (NexoEdu) — archivos que NO deben subirse a git
# ============================================================

# Dependencias
node_modules/
**/node_modules/

# Variables de entorno / SECRETOS (nunca subir)
.env
.env.*
**/.env
**/.env.*
!**/.env.example

# Salidas de build
dist/
**/dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sistema operativo / editores
.DS_Store
Thumbs.db
.idea/

# Carpetas de referencia (no forman parte del producto)
alpha-ref/
frontend/design-refs/

# Tests (excluidos del repo por decisión del equipo)
backend/tests/

# Datos semilla (PII de prueba + credenciales) — viven en el repo de BD, no aquí
backend/seed/

# Documentos internos de estudio/preparación (no forman parte del producto)
DEPLOYMENT.md
DOCUMENTACION_TECNICA.html

# README nuestro, guardado aparte para comparar con el del equipo (no subir)
README.nuestro.md
